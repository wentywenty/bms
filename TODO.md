# TWS BMS ROS 2 驱动 TODO 列表

## 🟢 已完成 (当前状态)
- [x] 基于 Modbus RTU 的串口通信基础框架。
- [x] 读取总电压、电流和 SOC（百分比）。
- [x] 修复了 Modbus 寄存器的 32 位字序（小端字序）解析。
- [x] 修正了带有 -40 偏移量的温度缩放。
- [x] **代码重构**: 将核心 Modbus 逻辑解耦为独立 C++ 库 (`tws_bms_lib`)。
- [x] **Python 绑定**: 集成 `pybind11` 提供 `tws_bms_api` Python 接口。
- [x] **故障位图解析**: 将 `protect_status` 映射到 ROS 健康状态常量。
- [x] **自动重连机制**: 实现串口异常断连后的自动恢复。
- [x] **静态信息读取**: 自动读取并缓存序列号 (SN)。
- [x] **版本与寿命日志**: 启动时自动读取并打印软件版本、硬件版本、SOH 和循环次数。
- [x] **软件关断服务**: 提供 `set_discharge_output` 服务控制放电 MOS。
- [x] **参数优化**: 新增 `publish_rate` 和 `design_capacity` 参数。
- [x] **二级过温保护**: 自动关断放电 MOS。
- [x] **终端诊断系统**: 彩色日志告警。
- [x] **udev 规则**: 固定设备名为 `/dev/bms`。

## 🟡 近期优化 (待处理)
- [ ] **单体电压**: 确认并实现 13 个电芯单体电压的读取。

## 🔴 高级功能
- [ ] **充电控制**: 增加 `set_charge_output` 服务。

## 🔵 维护
- [ ] 编写完整的 `README.md`。
- [ ] 优化 launch 文件。
